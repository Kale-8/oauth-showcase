# Secreto para firmar los JWT (usa una cadena larga y segura en producción)
JWT_SECRET=mi_secreto_super_seguro_para_jwt

# Tiempo de expiración del token
JWT_EXPIRES_IN=3600s

# Credenciales de Google OAuth2 (obtenerlas desde Google Cloud Console)
GOOGLE_CLIENT_ID=tu_google_client_id_aqui
GOOGLE_CLIENT_SECRET=tu_google_client_secret_aqui

# URL a la que Google redirigirá después del login
GOOGLE_CALLBACK_URL=http://localhost:3000/auth/google/redirect

# Puerto de la aplicación
PORT=3000

